/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["media.wired.com", "wijmqqiyvrwlzeetedxq.supabase.co"/*reference id (used for image publ)*/], 
  },
};

module.exports = nextConfig;
